@props([
    'title',
    'description',
])

<div class="mb-8">
    <h2 class="text-2xl font-bold text-gray-900 text-center mb-2">{{ $title }}</h2>
    <p class="text-sm text-gray-600 text-center">{{ $description }}</p>
</div>
